name: example
description: A new Flutter project.

publish_to: none

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cached_network_image_builder:
    path: ../

dev_dependencies:
  flutter_lints: ^1.0.4    

flutter:
  assets:
    - assets/images/error_image.png